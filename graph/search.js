class Search {
    /**
     * 
     * @param {Graph} g - Graph
     * @param {Node} s - source node
     */
    constructor(g, s){

    }

    /**
     * Return true if v connected to s
     * @param {Node} v 
     * @returns boolean
     */
    marked(v){

    } 

    /**
     * Returns no of connected vertices to s
     * @returns int
     */
    count() {

    }
}